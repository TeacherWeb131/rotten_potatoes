{% extends 'base.html.twig' %}

{% block title %}Connexion à l'application
{% endblock %}

{% block body %}
    <h1>Connexion</h1>
    {% if error %}
        {{ error.message }}
    {% endif %}
    <div class="alert alert-light">
        <form action="" method="post">
            <div class="form-group">
                <input class="form-control" name="_username" placeholder="Adresse email" type="email">
            </div>
            <div class="form-group">
                <input class="form-control" name="_password" placeholder="Mot de passe" type="password">
            </div>
            <button class="btn btn-success" type="submit">Connexion</button>
        </form>
    </div>
{% endblock %}
